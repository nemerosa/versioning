version: v1
configuration:
  defaults:
    branch:
      validations:
        GRADLE.PLUGIN: {}
        GITHUB.RELEASE: {}
      promotions:
        RELEASE:
          validations:
            - GRADLE.PLUGIN
            - GITHUB.RELEASE
